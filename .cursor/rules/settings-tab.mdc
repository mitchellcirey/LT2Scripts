---
description: Dashboard toggles stay session-only. Script settings stay on that script's page.
alwaysApply: true
---

# Settings

Ctrl Click, Disable shadows, Disable fog, and Always Day live in `Dashboard/Dashboard.lua`. They start on. Do not save them.

Script-specific settings stay on that script's settings page and in its `saveConfig`.

Keep the UI to a label and a control. No hint text.

```lua
-- BAD
Text = "Click the key, then press a new one."

-- GOOD
Text = "Toggle key"
```
